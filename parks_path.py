def algorithm(G, l, k):
    dist = [V][V]
    for each vertex v in V:
        dist[[v][v] = 0 #distance from v to v is 0
    for each edge (u, v) in l:
        dist[u][v] = l[u][v]
    for k = 1...|V|:
        
